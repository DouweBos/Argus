/**
 * Re-exports the Peacock Dialog primitive so app-level code can import from a
 * single stable path while the canonical implementation lives in @argus/peacock.
 */
export { Dialog } from "@argus/peacock";
export type { DialogProps } from "@argus/peacock";
